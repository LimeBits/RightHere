# RightHere 右键增强功能 PRD

## 背景

RightHere 当前核心能力是 Finder 右键“新建文件”。用户希望继续补齐 macOS Finder 在日常文件管理中不如 Windows 顺手的部分，让 RightHere 从“新建文件工具”扩展为“Finder 右键效率增强工具”。

本 PRD 聚焦一组高频、低打扰、适合 FinderSync 右键菜单承载的功能，并按批次规划后续版本。每个批次必须能独立验证，避免一次性把右键菜单做成杂货铺。

## 目标

- 在 Finder 右键菜单中提供更多高频效率操作。
- 优先解决 macOS 文件管理中常见的“路径深、入口远、操作绕”的问题。
- 功能必须默认克制，不让右键菜单变得臃肿。
- 涉及文件移动、删除、覆盖等有风险操作时，必须有明确状态、失败反馈和诊断记录。

## 非目标

- 不把 RightHere 做成完整 Finder 替代品。
- 不在第一批实现复杂文件管理器能力，例如完整剪切板状态 UI、批量重命名面板、压缩格式管理器。
- 不默认在右键菜单中展示过多低频 App 启动入口。

## 用户场景

### 场景 1：在当前文件夹打开终端

用户正在 Finder 中浏览项目目录，希望直接在当前目录打开 Terminal / iTerm / Warp，而不是手动打开终端再 `cd`。

### 场景 2：快速打开隐藏配置文件和常用位置

用户经常需要打开 `.zshrc`、`/etc/hosts`、`~/.codex`、`~/.ssh`、`~/.config`、`~/Library/Application Support/...` 等隐藏文件、系统配置文件或深层配置目录。这些位置 Finder 默认不容易到达，路径也不容易记住。用户希望把它们配置成“快捷打开”，之后通过右键菜单直接打开。

### 场景 3：开发工具中复制路径或文件名

用户需要把文件路径发给别人、贴到命令行、贴到 Markdown 或记录到项目文档中。此类功能应放入“开发工具”子菜单，避免和 Finder 原生“复制”“打开方式”等普通用户语义冲突。

### 场景 4：把文件移动或复制到快捷打开

用户下载了文件，希望右键后直接发送到“项目素材”“待处理”“归档”等固定目录，不想层层打开 Finder。该能力依赖“快捷打开”先稳定支持目录。

### 场景 5：类 Windows 的剪切 / 粘贴到这里

用户希望右键文件或文件夹后选择“剪切”，再到目标文件夹右键“粘贴到这里”，完成移动。

## 功能候选

### A. 在此处打开

在 Finder 右键菜单中增加“在此处打开”子菜单。

首批支持：
- 终端
- iTerm，如果已安装
- Warp，如果已安装
- Cursor，如果已安装
- VS Code，如果已安装

触发位置：
- 右键文件夹：以该文件夹作为工作目录。
- 右键文件夹背景：以当前 Finder 目录作为工作目录。
- 右键桌面背景：以桌面目录作为工作目录。

不触发位置：
- 右键单个文件时不显示，避免和 Finder 原生“打开方式”语义冲突。
- 多选文件或文件夹时第一版不显示，避免目录语义不清晰。

默认展示策略：
- “终端”默认开启。
- 第三方 App 仅在本机检测到已安装时展示。
- 设置页允许开关每个入口。

优先级：P0。

原因：高频、技术风险低、和 Finder 右键效率定位高度一致。

### B. 快捷打开

在设置页新增“快捷打开”配置，Finder 右键菜单展示“快捷打开”子菜单。

定位：
- 不是普通启动器。
- 不是 Finder 侧边栏的重复实现。
- 重点解决隐藏文件、隐藏目录、深层配置路径和个人高频工作目录难找的问题。

能力：
- 添加文件或文件夹
- 支持隐藏文件和隐藏目录
- 支持手动输入路径，例如 `~/.zshrc`、`/etc/hosts`、`~/.codex`
- 支持通过选择器添加文件或文件夹
- 重命名显示名称
- 排序
- 删除
- 打开快捷打开
- 在 Finder 中显示快捷打开
- 在菜单中隐藏某个快捷打开

默认内置建议：
- 用户主目录
- 下载
- 文稿
- 桌面

可选预设建议，但默认不强塞：
- `.zshrc`
- `/etc/hosts`
- `.codex`
- `.ssh`
- `~/Library/Application Support`

约束：
- 默认最多展示 8 个，避免右键菜单过长。
- 路径不存在时在设置页标记失效，右键菜单中不显示或置灰。
- 第一版只打开或显示，不做编辑、不做移动、不做复制。

优先级：P0。

原因：对普通用户不是绝对刚需，但对 RightHere 的目标用户非常有辨识度。隐藏配置文件和深层配置目录是 macOS 原生 Finder 的真实痛点，可能成为“一个功能就值得安装”的能力。与“在此处打开终端”可以共用设置页中的“右键工具”结构。

### C. 开发工具

在 Finder 右键菜单中增加“开发工具”子菜单，收纳偏开发者和路径处理的能力。

首批支持：
- 复制完整路径
- 复制文件名
- 复制不带扩展名文件名
- 复制所在文件夹路径
- 复制 Markdown 链接

触发位置：
- 右键文件：显示路径和文件名相关操作。
- 右键文件夹：显示路径和文件夹名相关操作。
- 右键文件夹背景：显示当前 Finder 目录路径相关操作。

多选策略：
- 多选时每行一个结果。
- Markdown 链接多选时每行一个链接。

优先级：P1。

原因：实现简单、开发和办公场景高频；但入口必须叫“开发工具”，不使用“复制”作为顶层菜单，避免和 Finder 原生菜单及普通用户认知冲突。

### D. 移动到 / 复制到快捷打开

基于“快捷打开”中的目录项，对选中文件或文件夹提供：
- 移动到快捷打开
- 复制到快捷打开

冲突策略：
- 第一版不覆盖原文件。
- 同名时自动追加序号，例如 `文件 (2).pdf`。
- 操作完成后显示通知，并记录诊断日志。

优先级：P1。

原因：比“剪切 / 粘贴到这里”更可控，目标目录来自用户预设，权限和失败处理更清晰。但必须等“快捷打开”本身稳定后再做。

### E. 从所选项目新建文件夹

右键选中文件后，提供“用所选项目新建文件夹”：
- 创建一个新文件夹
- 将所选文件移动进去
- 默认文件夹名可使用“新建文件夹”或当前日期

优先级：P2。

原因：Windows / Finder 用户都常用，和当前“新建文件”能力接近，但涉及移动文件，需谨慎实现。

### F. 剪切 / 粘贴到这里

类 Windows 文件剪切能力。

流程：
1. 用户右键文件或文件夹，选择“剪切”。
2. RightHere 记录待移动项目列表。
3. 用户在目标文件夹背景右键，选择“粘贴到这里”。
4. RightHere 执行移动。
5. 成功后清空待移动状态。

必要能力：
- 取消剪切
- 显示当前待粘贴数量
- 同名自动改名
- 失败时保留未完成项目
- 诊断日志记录源路径、目标路径、错误类型

风险：
- 文件移动是破坏性操作。
- iCloud、外接盘、网络盘、权限、同名冲突都会引发边界问题。
- Finder 原生剪切状态无法完全复刻，无法稳定显示灰显视觉状态。

优先级：P3。

原因：价值高但风险高，不适合在右键增强第一批实现。

### G. 打开常用 App

用户提到的“打开苹果备忘录、微信或其他 App”属于低频快捷启动。

建议：
- 不作为第一批重点。
- 如果实现，放在“打开方式 / 常用 App”子菜单。
- 默认不展示微信、备忘录等泛 App，避免右键菜单变成启动器。
- 可以支持用户自定义，但默认关闭。

优先级：P4。

原因：和 Finder 当前目录上下文关系较弱，刚需不如“在此处打开终端”。

## 优先级排序

| 优先级 | 功能 | 价值 | 风险 | 批次 |
| --- | --- | --- | --- | --- |
| P0 | 快捷打开：打开隐藏配置文件 / 常用位置 | 很高 | 低 | Batch 1 |
| P0 | 在此处打开终端 | 很高 | 低 | Batch 1 |
| P0.5 | 开发工具：复制路径 / 文件名 | 高 | 低 | Batch 2 |
| P1 | 在此处打开编辑器 | 高 | 中低 | Batch 2 |
| P1 | 移动到 / 复制到快捷打开 | 高 | 中 | Batch 3 |
| P2 | 从所选项目新建文件夹增强版 | 中 | 中 | Batch 4 |
| P3 | 剪切 / 粘贴到这里 | 高 | 高 | Batch 5+ |
| P4 | 打开常用 App | 低中 | 低 | 暂不规划 |

## 批次规划

### Batch 1：目录上下文和快捷打开基础

目标：先打通最刚需、低风险、可快速验证的右键效率基础。

包含：
- 设置页新增“右键工具”区域。
- 支持“快捷打开”管理。
- 快捷打开支持文件和文件夹。
- 支持手动输入路径，包含 `~` 展开和隐藏路径。
- 支持通过选择器添加文件或文件夹。
- Finder 右键菜单展示“快捷打开”。
- 点击快捷打开后打开文件、打开文件夹或在 Finder 中显示。
- 右键文件夹、文件夹空白处、桌面空白处时展示“在此处打开 -> 终端”。

不包含：
- 打开 Cursor / VS Code / iTerm / Warp。
- 复制路径。
- 文件移动、复制、剪切、粘贴。

验收：
- 可以添加并打开 `~/.zshrc`。
- 可以添加并打开 `/etc/hosts`。
- 可以添加并打开 `~/.codex`。
- 可以在项目文件夹空白处右键打开终端，终端工作目录正确。
- 右键单个文件时不显示“在此处打开”。

发布版本：0.2.0。

### Batch 2：开发工具和编辑器入口

目标：补齐开发者高频路径操作，并逐步扩展“在此处打开”的目标 App。

包含：
- “开发工具”子菜单。
- 复制完整路径。
- 复制文件名。
- 复制不带扩展名文件名。
- 复制所在文件夹路径。
- 复制 Markdown 链接。
- 检测已安装 Cursor / VS Code / iTerm / Warp。
- 设置页可开关每个编辑器入口。

不包含：
- 移动文件。
- 剪切粘贴。
- 泛 App 启动器。

验收：
- 右键文件时不显示“在此处打开”，但可显示“开发工具”。
- 多选文件复制路径时每行一个路径。
- 右键文件夹空白处可复制当前 Finder 目录路径。
- 已安装编辑器才出现在菜单或设置中。

建议版本：后续版本待定。

### Batch 3：移动到 / 复制到快捷打开

目标：基于已稳定的快捷打开，提供更可控的文件整理能力。

包含：
- 对选中文件或文件夹显示“移动到快捷打开”。
- 对选中文件或文件夹显示“复制到快捷打开”。
- 只显示快捷打开中的目录项，不显示文件项。
- 同名自动追加序号。
- 成功或失败后给出通知。
- 记录诊断日志。

不包含：
- 覆盖文件。
- 跨 App 拖拽。
- 剪切板式剪切/粘贴状态。

验收：
- 下载目录中的文件可移动到自定义归档目录。
- 同名文件不会覆盖。
- 快捷打开失效时不出现在移动/复制目标中。

建议版本：后续版本待定。

### Batch 4：文件夹整理增强

目标：只做比 Finder 原生能力更强的整理能力，不重复原生功能。

候选：
- 按日期新建文件夹并移动所选项目。
- 按扩展名归类所选项目。
- 从模板创建项目文件夹结构。

不做：
- 仅复制 Finder 原生“用所选项目新建文件夹”。

建议版本：后续版本待定。

### Batch 5+：剪切 / 粘贴到这里

目标：评估类 Windows 剪切/粘贴是否值得进入正式开发。

前置条件：
- Batch 3 的移动/复制能力稳定。
- 文件冲突处理稳定。
- 诊断日志足够完整。
- 用户确认仍强需求。

建议版本：后续版本待定。

## 第一批明确不做

Batch 1 不做：
- 移动文件
- 剪切粘贴
- 压缩解压
- 批量重命名
- 泛 App 启动器
- 编辑器入口
- 复制路径

原因：Batch 1 的目标是验证“快捷打开 + 终端”是否真的构成 RightHere 的第二个核心价值，不把风险较高的文件操作混进来。

## 信息架构

设置页建议新增或调整为：

- 模板
- 右键工具
- 更新
- 高级

“右键工具”中包含：
- 在此处打开
- 快捷打开
- 开发工具，Batch 2 加入
- 移动到快捷打开，Batch 3 加入

## 右键菜单结构建议

首阶段菜单结构：

```text
新建文件
在此处打开
  终端
快捷打开
  .zshrc
  hosts
  Codex 配置
  下载
```

后续扩展：

```text
开发工具
  完整路径
  文件名
  Markdown 链接
移动到
  项目
  归档
复制到
  项目
  归档
```

## 技术方案概览

### FinderSync 菜单入口

沿用现有 FinderSync extension：
- 根据右键对象判断是文件、文件夹还是背景。
- 根据设置生成对应菜单项。
- 点击菜单项后执行打开、复制或移动逻辑。

### 设置共享

沿用 App Group UserDefaults：
- 保存快捷打开列表。
- 保存工具开关。
- 保存第三方 App 检测结果或用户偏好。
- 设置变更后通过分布式通知同步给 FinderSync extension。

### 打开终端

可选实现：
- Terminal：通过 `open -a Terminal <目录>` 或 AppleScript。
- iTerm / Warp：检测 App 是否存在，再按各自支持方式打开目录。
- Cursor / VS Code：优先使用 URL scheme 或 CLI；没有 CLI 时退回打开 App。

第一版优先支持系统 Terminal，第三方编辑器按检测情况逐个加入。

### 快捷打开

主 App 中用 NSOpenPanel 添加文件或目录，同时支持手动输入路径。

保存字段：
- id
- displayName
- path
- kind: file / directory / unknown
- enabled
- sortOrder
- openMode: open / revealInFinder

路径处理：
- 支持 `~` 展开。
- 支持隐藏文件和隐藏目录。
- 支持路径不存在时保留配置，但标记失效。
- 文件项点击后用默认 App 打开，或在 Finder 中显示。
- 目录项点击后在 Finder 中打开。

如果未来重新开启主 App sandbox，需要保存 security-scoped bookmark。当前 RightHere 正式分发主 App 不使用 App Sandbox，但 extension 仍要注意目标目录权限。

## 成功指标

- 用户拖拽安装后，无需额外命令即可看到新菜单。
- 在常见目录右键，打开终端成功率接近 100%。
- 快捷打开添加、删除、排序后，下次右键菜单立即同步。
- 隐藏文件和深层配置目录可以通过快捷打开打开。
- 右键菜单不超过两层主要层级，不明显变慢。
- 诊断信息能看出菜单生成、点击动作和失败原因。

## 待确认问题

- Batch 1 是否只内置用户主目录、下载、文稿、桌面，还是提供 `.zshrc`、`hosts`、`.codex` 作为可选预设？
- 快捷打开点击文件时默认“打开文件”，还是默认“在 Finder 中显示”？
- 快捷打开是否支持图标或仅显示名称？
- 右键菜单里“在此处打开”和“快捷打开”是否放在“新建文件”下面？
- Batch 2 的“开发工具：复制路径”是否提前并入 Batch 1？

## 结论

最高优先级是“快捷打开”和“在此处打开终端”。其中“快捷打开”不是 Finder 侧边栏的重复能力，而是面向隐藏文件、系统配置文件和深层配置目录的快速入口，具备更强的产品辨识度。

“开发工具：复制路径 / 文件名”价值高、风险低，应作为第二批或提前加入第一批的候选。“剪切 / 粘贴到这里”价值很高，但应排在后续版本。它涉及真实移动文件，必须等右键工具体系、设置同步、诊断日志和失败处理更稳定后再做。
